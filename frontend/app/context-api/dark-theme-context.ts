// import { createContext, useContext, useState } from "react";

// const toggleTheme = (dark: null | string) => {
//   dark = dark === null ? "dark" : null;
// };

// const DarkContext = createContext(toggleTheme);

// const DarkProvider = ({ children }) => {
//   // const [dark, setDark] = useState();

//   return (
//     <DarkContext.Provider value={toggleTheme}>{children}</DarkContext.Provider>
//   );
// };

// export const useDarkContext = () => useContext(DarkContext);
// export default DarkProvider;
